## Plan

- Update the hero layout so the 3D constellation/canvas is rendered only on desktop breakpoints.
- Remove or hide the mobile/tablet 3D canvas/fallback section so the “third” visual no longer appears below the text on narrow screens.
- Keep the existing desktop right-side constellation behavior unchanged.
- Verify at mobile width (~390px) and tablet width that the 3D visual is absent, and verify desktop still shows it on the right.